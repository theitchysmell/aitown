# AI Town Server

This is a minimal Express server that provides a simple API for managing AI agents.

## Running the server

Install dependencies and start the server:

```bash
npm install
npm start
```

The server listens on port `3001` by default.
