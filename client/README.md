# AI Town Client

This is a minimal client built with React (loaded from a CDN) that interacts with the AI Town server.

## Running the client

Serve the `client` folder with any static file server. For development, you can use `npx serve` or similar tools.

```bash
npx serve -l 3000
```

The client expects the API server to be running on `http://localhost:3001`.
